<p id="notice"><%= notice %></p>

<div class="container">
  <div class="bg-faded p-4 my-4">
    <!-- Welcome Message -->
    <div class="text-center mt-4">
      <h1 class="my-2">Albums <%= image_tag('Albums/logo.jpg', height:'52', width:'52') %></h1>
        <hr class="divider">
        <h2 class="text-center text-lg text-uppercase my-0">If you want, you can:
        <strong><%= link_to 'Create Album', new_album_path %></strong>
        </h2>
      </div>
    </div>
  </div>
  <hr class="divider">
  <h2 class="text-center text-lg text-uppercase my-0">Or
  <strong>Explore</strong>
  </h2>
  <hr class="divider">

  <div id="gallery">
    <ul id="grid">
      <% @albums.each do |album| %>
          <li><%= link_to image_tag(album.image), album%><%=album.title%></li>
      <% end %>
    </ul>
  </div>
<!--
  <table class="table table-bordered">
    <thead>
      <tr>
        <th></th>
        <th>Title</th>
        <th>Artist</th>
        <th>Genre</th>
        <th colspan="2"></th>
      </tr>
    </thead>

    <tbody>
      <% @albums.each do |album| %>
        <tr>
          <td><%= link_to image_tag(album.image, width:'25', height:'25'), album%></td>
          <td><%= link_to album.title, album %></td>
          <td><%= link_to "#{album.artist.name}", album.artist %></td>
          <td><%= album.genre.name %></td>
          <% if album.user == current_user %>
          <td><%= link_to image_tag("Songs/trash.png", width:'25', height:'25'), album, method: :delete, data: { confirm: 'Are you sure?' } %>
          <%= link_to image_tag("Songs/edit.svg", width:'20', height:'20'), edit_album_path(album) %></td>
          <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>
-->
