<p id="notice"><%= notice %></p>

<div class="container">
  <div class="bg-faded p-4 my-4">
    <!-- Welcome Message -->
    <div class="text-center mt-4">
      <h1 class="my-2">Artists <%= image_tag('Artists/img1.jpg', height:'72', width:'72') %></h1>
        <hr class="divider">
        <h2 class="text-center text-lg text-uppercase my-0">If you want, you can:
        <strong><%= link_to 'Create Artist', new_artist_path %></strong>
        </h2>
      </div>
    </div>
  </div>
  <hr class="divider">
  <h2 class="text-center text-lg text-uppercase my-0">Or
  <strong>Explore</strong>
  </h2>
  <hr class="divider">

  <div id="gallery">
    <ul id="grid">
      <% @artists.each do |artist| %>
          <li><%= link_to image_tag(artist.image), artist%><%=artist.name%></li>
      <% end %>
    </ul>
  </div>
<!--
  <table class="table table-bordered">
    <thead>
      <tr>
        <th></th>
        <th>Name</th>
        <th>Genre</th>
        <th>Options</th>
      </tr>
    </thead>
    <tbody>
      <% @artists.each do |artist| %>
        <tr>
          <td><%= link_to image_tag(artist.image, width:'25', height:'25'), artist%></td>
          <td><%= link_to artist.name, artist %></td>
          <td><%= artist.genre.name unless artist.genre.nil? %></td>
          <% if artist.user == current_user%>
          <td><%= link_to image_tag("Songs/trash.png", width:'25', height:'25'), artist, method: :delete, data: { confirm: 'Are you sure?' } %>
          <%= link_to image_tag("Songs/edit.svg", width:'20', height:'20'), edit_artist_path(artist) %></td>
          <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>
-->
